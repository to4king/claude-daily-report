# CLAUDE.md — claude-daily-report 運用ルール

このファイルは Routines セッションで自動適用される運用ルールです。

## 情報収集の方針

- **対象ソース（公式のみ）**:
  - Anthropic News: https://www.anthropic.com/news
  - Claude ドキュメント リリースノート: https://docs.claude.com
  - Claude Code 変更履歴: https://code.claude.com
- `anthropic.com/news` と `docs.anthropic.com` は HTTP 403 が返ることがある。その場合は WebSearch / GitHub Releases / PyPI に切り替える
- 公式以外の二次情報（個人ブログ・SNS・まとめサイト）は採用しない
- 発表日が前日であることを必ず確認する。古い記事を新着として扱わない

## レポート作成の方針

- すべての成果物（レポート本文・Slack 通知）は **日本語** で作成する
- 実行日は **JST（日本標準時）** 基準とする
- 不確実な情報は「未確認」と明記し、憶測で新機能として書かない

### 新発表があった場合
各発表について「タイトル / 概要（3〜5行）/ 公式リンク / 実務での使いどころ（1〜2行）」を記載。

### 新発表がなかった場合
以下の 3 カテゴリから合計 3 つを選び、活用 Tips を作成:
- (a) スラッシュコマンド
- (b) 標準スキル
- (c) 著名プラグインの活用事例

各項目「名称 / 何ができるか / 具体的な使用例（コマンド例含む）/ 出典リンク」を記載。
各 Tips は「仕組み・コマンド例・使い場面・注意点」の構成で 10 行規模に詳述する。
毎日同じ内容にならないよう、一般的でない・実務的なものを優先する。

## Google Drive 保存

- ファイル名: `claude-daily-YYYY-MM-DD.html`（実行日の日付）
- Drive MCP のアップロード上限（約 200KB）のため HTML 形式で保存する
- PDF 変換が必要な場合は `html_to_pdf.js` を使用するが、現状は HTML で代替

## Slack 通知

- 形式:
  ```
  📋 Claude日次レポート(YYYY-MM-DD)
  （本文の3行要約）
  （Drive ファイルへのリンク）
  ```
- 区切り線（`---`）は使わない（Slack で表示エラーになるため）

## エラー時の方針

- 公式ソースにアクセスできない場合はその旨を明記して Slack に通知し、取得できた範囲でレポートを作成する
